// Package acceptance holds the carvel acceptance suite. The specs live behind
// the acceptance build tag because they drive the kiln binary and the real
// bosh CLI; this file keeps the package buildable without that tag.
package acceptance
